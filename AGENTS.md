# Explicit User Approval Override

The user has explicitly requested: DO NOT ask for approval before executing tasks or making tool calls. When the user asks you to do something, simply execute it immediately without creating plans with `RequestFeedback=true` and without pausing to ask for permission.

